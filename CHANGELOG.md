# 更新日志

## v1.6.1

**优化**

- PR 日志导入：生成「加入技能队列」的目标从固定「自身」改为按技能表 EffectRange 自动解析，与游戏内编辑器的「自动 (Auto)」一致——射程 0=自身，其余（含近战，表内为 -1）=当前目标；查不到射程数据时回退自身
- `data/actions.json` 新增射程字段 `r`（Action.exh 第 15 列 EffectRange，sbyte），仅游戏本体导出路径提供；MCP/CSV 兜底路径无可靠列映射，导出的记录不带 `r`

## v1.6.0

**新功能**

- PR「▶ 模拟测试」新增 FFLogs 报告来源：导入向导顶部切换 ACT 本地日志 / FFLogs 报告，FFLogs 路径三步（报告链接或 CODE → 选择战斗 → 下载并模拟），与 ACT 路径共享同一套内嵌模拟视图（锚点/行为组行内徽章、顶部摘要条、SyncLog 格式模拟日志、改锚点自动重模拟）
- FFLogs 战斗边界（开怪/击杀时刻）直接作为模拟起止点，比 ACT 的开怪点推断更准；只下载敌方施法数据（hostility=1），带分页进度与取消
- 敌方来源按报告实体自动过滤宠物（Pet），默认全选可改；同键 1s 去重防读条刷新干扰匹配；技能名过国服中文名库翻译

## v1.5.0

**新功能**

- 战斗日志新增「ACT」导入（工具栏 FFLogs 旁）——无需联网与报告代码，直接读取本机 ACT 日志（`FFXIVLogs/Network_*.log`）导入 BOSS 事件与玩家技能使用；行解析参考 ccinos/act_dps_show，在其仅取 21 行（Ability）之外补全 20 行（StartsCast，与 cast 配对得出真实读条时长）与 22 行（AOE，按目标拆分的多行去重为单个事件）
- ACT 导入向导四步：① 选择日志文件（自动探测呆萌整合版 `C:\Tools\ACT.DieMoe\FFXIVLogs` 与官方 `%APPDATA%\Advanced Combat Tracker\FFXIVLogs`，目录可改并持久化，也可浏览任意文件）→ ② 选择战斗（按活动时间间隔自动分段，间隔可调；只保留有敌方单位参与的连续活动，自动过滤主城/野外零散动作；列表显示区域名、开始时刻、时长与事件数）→ ③ 解析选项（事件/能力技/GCD/读条时长/过滤正则，与 FFLogs 导入一致）→ ④ 映射导入（与 FFLogs 导入共用映射界面和替换/合并逻辑）
- 大文件流式解析：100MB+ 的全天日志扫描/窗口解析均在 1 秒内完成，带进度条与取消；窗口解析越过时间窗即提前结束读取
- 玩家来源显示职业（AddCombatant 读取，非法职业数据自动过滤回退「冒险者」）；玩家召唤兽/化身（朝日小仙女/烈日巴哈姆特/宝石兽等）按归属者自动识别为「宠物」排在事件来源末尾，不参与 BOSS 启发式标记，避免默认勾选被宠物施法带偏
- ACT 技能名按技能 ID 过国服中文名库统一（可修正 ACT 未识别的 `unknown_xxx` 技能名）；BOSS 事件图标解析与 FFLogs 导入相同
- PR：新增「▶ 模拟测试」——导入某场 ACT 日志战斗，按 PromeRotation 运行时语义逐帧回放事件流（SyncMatcher 窗口/参数匹配/一事件一匹配/ForceJump、SegmentTracker 段内激活、时钟命中硬拉 JumpTargetTime 等逐行对齐插件源码），检验时间轴锚点同步与行为组激活是否与真实战斗一致
- 模拟结果直接内嵌时间轴视图：锚点行显示 命中（含时钟校正量 Δ）/过期/未等到/永不触发 徽章，行为组行显示 激活时刻/未到达/段跳过/越界 徽章；顶部摘要条统计通过率，可展开与插件 SyncLog 同格式的模拟日志（可与游戏内 PRLog 逐条对照）
- 同一份日志可反复模拟：修改/增删锚点或行为组后自动重新计算，无需重新导入；结果点击即达对应锚点/行为组，改完立刻看到影响
- 模拟事件流只保留非宠物敌方来源（玩家与召唤物自动过滤），来源默认全选可改；开怪点自动判定为首个非宠物敌方动作或玩家指向敌方的技能，避免 ACT 分段起点混入开怪前准备动作导致整场偏移
- 无法从 ACT 日志观察的同步类型（Weather/ChatLog/Countdown 等）与运行时永不命中的类型（ActorControl/Lua/Manual）在结果中明确标注，不与真正的过期混淆

**内部**

- 新增模拟内核纯函数 `pr/sim/simEngine.ts` 与事件流组装 `pr/sim/simEvents.ts`，36 个单测覆盖窗口边界/默认窗口/ForceJump/阶段清理/回跳不重放/开怪点判定等关键语义

## v1.4.1

**修复**

- FFLogs 导入：修复映射步骤（第 ④ 步）显示整个报告所有实体的问题——报告中含多场战斗时（如整场 PvP 阵地战记录），玩家/BOSS/NPC 列表不再混入其他战斗的实体，只保留所选战斗时间窗内实际有施法记录的实体，来源选择列表大幅精简

## v1.4.0

**新功能**

- 设置中新增「界面」区块：字体大小六档可调（80% / 90% / 100% / 110% / 125% / 150%），选择后立即生效并保存，重启后保持；整个界面（工具栏/侧栏/视图/属性面板/状态栏）等比缩放，脚本编辑器等代码字体同步跟随
- 模式切换保留工作现场：AE / PR / 战斗日志三个模式之间切换时，各模式记住自己之前的工作位置——已打开的文件、滚动位置、树节点展开状态、PR 筛选文本、脚本编辑器光标与撤销历史全部原样保留，切换回来后直接从之前的地方继续，无需重新找位置

**优化**

- 切换编辑模式时窗口标题同步为当前模式正在编辑的文件名

## v1.3.0

**新功能**

- PR：新增「📥 日志导入」——把战斗日志文档中的玩家技能（GCD 轨道 + 能力技列）一键导入为 PR 时间轴行为组，每个技能生成一个「加入技能队列」（enqueueskill）行为组，目标默认自己，GCD/能力技自动区分 SkillType
- 分段锚点对齐算法：多阶段副本中不依赖战斗绝对时间——用锚点自身的同步规则（CastStart/ActionEffect 的技能 ID / Regex）在日志 BOSS 事件中单调顺序匹配，找回每个锚点的真实发生时刻；同名技能多次出现时按期望窗口（±30s）自动对到正确的第 k 次
- 区分读条/判定：CastStart 锚点对齐读条开始时刻（判定时刻 − 读条时长），ActionEffect 锚点对齐判定时刻
- 技能落位段内缩放校正：按「日志段时长 / PR 段时长」比例换算 Offset，转场挂机期/节奏漂移自动收敛；Offset 越界时小偏差钳制段内、大偏差改挂下一锚点，全程满足 PR 校验规则
- 导入向导三步：① 选择日志文档 → ② 锚点映射表（精确=绿 / 插值=黄 / 外推=红，可对每个锚点下拉人工改选对应事件，锚点旁标注读条/判定）→ ③ 按锚点分组的落位预览（钳制标黄、跳过标红），确认后整批导入，撤销一次即可整体回退
- 无法从日志观察的锚点（Manual/Lua/Countdown 等）自动用前后锚点分段线性插值，尾部外推，并在映射表中标出供人工核对

## v1.2.1

**优化**

- 战斗日志空文档引导：未打开文件时中央面板显示三步引导卡片（① 新建/打开文件 → ② 选择技能 → ③ 导入 FFLogs），每步附说明与快捷按钮；新建文件后自动切换到左侧「技能」Tab，衔接下一步操作
- 技能面板联动提示：无文档时「技能」Tab 顶部显示引导横幅，技能库与自定义技能的添加按钮禁用并提示「（先新建文件）」，避免误操作无反馈

## v1.2.0

**新功能**

- 新增「战斗日志」编辑模式（工具栏第三个模式）——垂直技能时间轴，用于按 BOSS 读条安排技能轴
- FFLogs 导入向导（四步）：报告链接/代码 → 选择战斗与 BOSS → 选择技能来源（指定玩家/指定技能）→ 预览并导入；支持替换或合并到当前文档，支持技能别名匹配（matchName）
- BOSS 事件时间轴：图标 + 读条矩形块，重合读条自动分配多个并列轨道；读条时长由 begincast/cast 配对得出
- 技能列管理：从 FFLogs 导入或从职业技能库添加；每列可显示/隐藏、改名、删除；GCD 自动归入独立轨道
- 职业技能库：21 职业 985 技能（含冷却/持续时间/图标，数据来源 xivapi + 盛趣 CDN），按职业浏览、搜索、点击添加为技能列
- 技能 CD 灰色长条从使用点向下延伸，直观展示冷却覆盖区间
- 战斗日志文档独立目录管理（默认 `文档/TimelineEditor/LogsTimelines`，可在文件 Tab 中更改），支持新建/打开/保存/另存为

**修复与优化**

- FFLogs 导入：技能名按 guid 经国服客户端数据（本地库 `data/action-names-cn.json`，45372 条）译为中文——修复 FFLogs 对 7.x 新技能缺中文翻译导致 BOSS 技能显示英文、圣盾阵/极致防御/暗影卫等新减伤无法匹配导入的问题
- 国服客户端中加密占位（`_rsv_`）的技能名支持人工解密覆盖表 `data/rsv-overrides.txt`（`_rsv_占位串|中文名` 逐行追加，按整串精确匹配防表间 id 冲突），已内置绝妖星绝境战全量 118 条
- BOSS 事件图标同样接入本地库图标 id（xivapi 静态 CDN，不受其 API 数据冻结影响）
- BOSS 事件区分读条/瞬发：有 begincast 配对的 cast 才画读条矩形（真实时长），无配对的瞬发技能只画图标 + 刻度线；事件属性面板可手工设置读条时长（留空 = 瞬发）
- 编辑模式切换改为三个并列按钮（AE 时间轴 / PR 时间轴 / 战斗日志），点击直达目标模式，当前模式以对应配色高亮
- 战斗日志全面标注技能 ID：BOSS 事件与 GCD/能力技悬停提示、列头提示、属性面板副标题均显示 ID；事件/GCD 使用/技能列的属性面板支持手工填写；FFLogs 导入按 guid 自动填充，技能库添加的列经中文名库反查自动填充
- FFLogs API 错误现在显示接口返回的具体错误信息（如报告不存在、权限不足），而非笼统的 HTTP 状态码

## v1.1.4

**新功能**

- PR：新增「检测自身职业」条件（selfjob）——按职业类别（T/奶/近战DPS/远敏DPS/法系DPS）或按具体职业（23 个职业含 7.56 驯兽师）下拉选择，仅显示当前方式所需的选项
- PR：新增「加入技能组」动作（enqueueskillgroup）——技能列表行编辑器：技能 ID + 自动显示技能名 + 类型/目标下拉 + 上移/下移/删除；输入技能 ID 时按技能表自动识别 GCD/能力技（与游戏内编辑器 Auto 行为一致，仍可手动修改）
- PR：新增「横幅消息提示」动作（showhint）——消息内容、显示时间（0.5~30 秒）、蓝色（提示）/红色（警告）样式

以上类型与默认值均对齐 PromeRotation 本体 2026-09-17 更新（字段严格按源码 `ToDto()` 序列化）。

## v1.1.3

**新功能**

- AE：新增「时间轴信息」编辑面板（未选中节点时在右侧编辑 Name / Author / TargetJob / TerritoryTypeId / TerritoryWeatherId / TargetAcrAuthor / Note / ExposedVars / LogsAddress / GUID，支持重新生成 GUID）
- AE：起手脚本（OpenerScript）可在底部 Monaco 脚本面板中编辑
- PR：元数据面板补全——Variables 变量列表编辑、起手模板（Opener）、备注
- PR：自定义起手脚本（Meta.CustomOpener.Script）Monaco 编辑，留空自动省略字段
- PR：「C# 持续行为」节点的脚本改为在底部 Monaco 面板中编辑
- PR：跨锚点 / 跨行为组复制粘贴——Ctrl+C / Ctrl+V，或行为组行 📋、锚点行「📋粘贴」、节点右键菜单；行为组粘贴到目标锚点时 Offset 自动收敛进段窗口，节点子树粘贴时自动重排 Id

**修复与优化**

- 修复脚本面板仅打开就幻影标脏并压入空撤销记录的问题
- 职能检测（timelinerole）下拉移除会导致条件恒假的 None 选项，并提示游戏内需设置「当前职能」（插件面板下拉或 /e MT）
- 多项 UI/UX 细节优化（对话框、右键菜单、状态栏、侧栏、工具栏等）

**内部**

- 撤销栈支持同 tag 连续编辑合并为一步
- DEV 模式暴露 `window.__prStore` 便于自动化验证
